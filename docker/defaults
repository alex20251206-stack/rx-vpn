# Baked into the image; overwritten at container start by scripts/docker-cmd.sh from
# docker-compose environment (OVPN_REMOTE_HOST from .env, etc.).
OVPN_REMOTE_HOST=127.0.0.1
OVPN_REMOTE_PORT=443
STUNNEL_ACCEPT=443
PANEL_PUBLIC_SCHEME=http
PANEL_PUBLIC_PORT=8139
